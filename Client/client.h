#include <iostream>
#include <pthread.h>
#include <string>

using std::cout;
using std::endl;
using std::cin;
using std::string;
using std::getline;

void ShowMenu();

int GetUserInput();

string GetTitle();

string StringFormatter(string rawString);